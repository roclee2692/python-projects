# 决策树与随机森林 - 练习题

## 练习1：简单决策树（基础）

### 题目描述
使用虹膜数据集训练一棵决策树。

### 任务
```python
from sklearn.datasets import load_iris
from sklearn.tree import DecisionTreeClassifier, plot_tree

data = load_iris()
X = data.data
y = data.target

# 1. 划分训练集和测试集
# 2. 训练深度为3的决策树
# 3. 计算准确率
# 4. 可视化树结构
# 5. 打印特征重要性
```

---

## 练习2：过拟合分析（进阶）

### 题目描述
通过改变树的深度，观察过拟合现象。

### 任务
```python
# 1. 生成分类数据
# 2. 训练深度为 1, 2, 3, ..., 15 的决策树
# 3. 记录每个模型的训练准确率和测试准确率
# 4. 绘制两条曲线
# 5. 分析：
#    - 在哪个深度开始过拟合？
#    - 最优深度是多少？
#    - 为什么会过拟合？
```

---

## 练习3：随机森林基础（实战）

### 题目描述
使用随机森林分类乳腺癌数据集。

### 任务
```python
from sklearn.datasets import load_breast_cancer
from sklearn.ensemble import RandomForestClassifier

data = load_breast_cancer()
X = data.data
y = data.target

# 1. 划分数据集
# 2. 训练随机森林（100棵树，max_depth=10）
# 3. 计算准确率、精确率、召回率、F1
# 4. 绘制混淆矩阵
# 5. 绘制ROC曲线
# 6. 找出Top 5重要特征
# 7. 讨论：随机森林为什么比单棵树好？
```

---

## 练习4：树的数量影响（进阶）

### 题目描述
研究随机森林中树的数量对性能的影响。

### 任务
```python
# 1. 固定max_depth=5
# 2. 训练n_estimators为 1, 5, 10, 20, 50, 100, 200的模型
# 3. 记录每个模型的测试准确率
# 4. 绘制n_estimators vs 准确率的曲线
# 5. 分析：
#    - 准确率如何随树数量增加？
#    - 是否有收敛？
#    - 几棵树就够了？
#    - 为什么？
```

---

## 练习5：超参数调优（高级）

### 题目描述
使用GridSearchCV找到最优超参数。

### 任务
```python
from sklearn.model_selection import GridSearchCV
from sklearn.ensemble import RandomForestClassifier

# 定义参数网格
param_grid = {
    'n_estimators': [50, 100, 200],
    'max_depth': [5, 10, 15, 20],
    'min_samples_split': [2, 5, 10],
    'min_samples_leaf': [1, 2, 4]
}

# 1. 使用GridSearchCV进行网格搜索（5折交叉验证）
# 2. 找到最优参数组合
# 3. 打印最佳参数和最佳交叉验证分数
# 4. 用最优模型评估测试集
# 5. 对比与默认参数的性能差异
```

---

## 练习6：决策树可视化（综合）

### 题目描述
理解决策树的内部结构。

### 任务
```python
# 1. 加载虹膜数据集（只用前两个特征，便于可视化）
# 2. 训练深度为3的决策树
# 3. 可视化树结构（使用plot_tree，设置filled=True）
# 4. 手动追踪一个测试样本的分类过程
# 5. 分析：
#    - 根节点选择的是哪个特征？为什么？
#    - 每次分割的标准是什么？
#    - 如何从树中读取决策规则？
```

---

## 练习7：多分类决策树（高级）

### 题目描述
使用决策树处理多分类问题。

### 任务
```python
# 1. 加载虹膜数据集（完整3个类别）
# 2. 训练深度为4的决策树
# 3. 计算整体准确率和每个类别的精确率/召回率
# 4. 绘制混淆矩阵（3x3）
# 5. 特征重要性分析
# 6. 与逻辑回归对比：
#    - 准确率对比
#    - 可解释性对比
#    - 计算速度对比
```

---

## 练习8：随机森林特征重要性（项目）

### 背景
你是一家银行的数据科学家，需要识别预测客户流失最重要的因素。

### 任务
```python
# 1. 使用真实的客户流失数据集（自行寻找或构造）
# 2. 训练随机森林分类器
# 3. 提取特征重要性
# 4. 可视化Top 10特征
# 5. 业务解读：
#    - 哪些因素最影响客户流失？
#    - 如何改进产品/服务？
#    - 优先关注哪些客户？
# 6. 与逻辑回归的系数对比
# 7. 写一份简短的分析报告
```

---

## 挑战题：自定义决策树的分割标准

### 题目
用纯信息增益（而不是基尼指数）训练决策树，对比结果。

### 任务
```python
from sklearn.tree import DecisionTreeClassifier

# 1. 使用criterion='entropy'训练（基于信息增益）
# 2. 对比criterion='gini'和criterion='entropy'
# 3. 可视化两棵树
# 4. 对比：
#    - 树的结构是否不同？
#    - 性能是否不同？
#    - 为什么？
```

---

## 提交要求

对于每道练习题：
1. 编写完整的Python代码
2. 添加详细的注释
3. 包含数据可视化
4. 写出分析和结论

完成后对照 `04-答案详解.py` 检查答案。

## 学习建议

- **练习1-2**：适合初学者，巩固基础
- **练习3-5**：适合有一定基础，想深入理解
- **练习6-7**：适合准备实际应用
- **练习8-挑战题**：适合追求深度理解的学习者

**记住：决策树是黑盒模型吗？不是！它是所有模型中最容易解释的！**

