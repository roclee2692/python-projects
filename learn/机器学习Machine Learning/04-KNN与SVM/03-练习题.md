# KNN与SVM - 练习题

## 练习1：KNN基础（基础）

### 题目描述
使用虹膜数据集训练KNN分类器。

### 任务
```python
from sklearn.datasets import load_iris
from sklearn.neighbors import KNeighborsClassifier
from sklearn.preprocessing import StandardScaler

data = load_iris()
X = data.data
y = data.target

# 1. 划分训练集和测试集
# 2. 使用StandardScaler标准化特征
# 3. 用K=3训练KNN
# 4. 计算准确率
# 5. 手动验证：选择一个测试样本，找到3个最近的训练样本
#    - 使用KNN的属性：model._fit_X, model._fit_y, model.kneighbors()
```

---

## 练习2：K值的影响（进阶）

### 题目描述
研究K值如何影响KNN的性能。

### 任务
```python
# 1. 使用虹膜数据集
# 2. 训练K值为 1, 3, 5, 7, 9, 15, 20 的KNN模型
# 3. 记录每个模型的训练准确率和测试准确率
# 4. 绘制两条曲线
# 5. 分析：
#    - 在什么K值时测试准确率最高？
#    - K=1和K=N有什么区别？
#    - 为什么K太小会过拟合？
#    - 为什么K太大会欠拟合？
```

---

## 练习3：特征缩放的重要性（实战）

### 题目描述
展示为什么KNN需要特征缩放。

### 任务
```python
# 生成数据
import numpy as np
X = np.array([
    [1, 100],
    [2, 200],
    [3, 300],
    [100, 1],
    [200, 2],
    [300, 3]
])
y = np.array([0, 0, 0, 1, 1, 1])

# 1. 在未缩放的X上训练KNN（K=3）
# 2. 在缩放的X上训练KNN（K=3）
# 3. 对比结果
# 4. 分析：为什么缩放很重要？

# 提示：你会发现，未缩放时，第二个特征（100倍大）完全主导距离计算
```

---

## 练习4：SVM线性可分数据（进阶）

### 题目描述
使用SVM分类线性可分的数据。

### 任务
```python
# 生成线性可分数据
np.random.seed(42)
X = np.vstack([
    np.random.randn(100, 2) + [2, 2],
    np.random.randn(100, 2) + [-2, -2]
])
y = np.hstack([np.ones(100), np.zeros(100)])

# 1. 划分训练集和测试集
# 2. 标准化
# 3. 用线性核训练SVM（kernel='linear'）
# 4. 绘制决策边界
# 5. 尝试不同的C值：[0.1, 1, 10]，对比决策边界

# 思考：C越大，间隔是否越大？为什么？
```

---

## 练习5：SVM非线性数据（高级）

### 题目描述
使用SVM分类非线性数据。

### 任务
```python
# 生成非线性数据（同心圆）
from sklearn.datasets import make_circles

X, y = make_circles(n_samples=300, noise=0.1, factor=0.3, random_state=42)

# 1. 划分数据集
# 2. 标准化
# 3. 分别用以下核训练SVM：
#    - Linear（线性）
#    - RBF（高斯）
#    - Poly（多项式）
# 4. 绘制三个决策边界
# 5. 对比准确率
# 6. 分析：为什么线性核效果差？为什么RBF和Poly效果好？
```

---

## 练习6：SVM超参数调优（综合）

### 题目描述
使用GridSearchCV找到最优的SVM超参数。

### 任务
```python
from sklearn.model_selection import GridSearchCV
from sklearn.svm import SVC

data = load_breast_cancer()
X = data.data
y = data.target

# 1. 划分和缩放数据
# 2. 定义参数网格：
#    - kernel: ['linear', 'rbf']
#    - C: [0.1, 1, 10]
#    - gamma (仅对RBF): ['scale', 0.001, 0.01, 0.1]
# 3. 使用GridSearchCV找最优参数（5折交叉验证）
# 4. 打印最优参数和最佳交叉验证分数
# 5. 用最优模型评估测试集
```

---

## 练习7：KNN vs SVM对比（高级）

### 题目描述
对比KNN和SVM在不同数据集上的性能。

### 任务
```python
# 使用三个数据集：
# 1. 虹膜数据集（简单）
# 2. 乳腺癌数据集（中等）
# 3. 自生成复杂非线性数据

对每个数据集：
  - 训练KNN（K=3和K=5）
  - 训练SVM（Linear、RBF、Poly）
  - 对比准确率
  - 记录训练时间和预测时间
  - 绘制对比表格
```

---

## 练习8：维数诅咒（项目）

### 背景
理解为什么KNN在高维空间中性能下降。

### 任务
```python
# 1. 生成高维数据
np.random.seed(42)
n_samples = 1000

# 创建不同维度的数据
for n_features in [2, 5, 10, 20, 50, 100]:
    X = np.random.randn(n_samples, n_features)
    y = (X[:, 0] + X[:, 1] > 0).astype(int)
    
    # 训练KNN（K=5）
    # 记录准确率
    
# 2. 绘制维度 vs 准确率
# 3. 分析：为什么高维空间中性能下降？
# 4. 解决方案：
#    - 特征选择
#    - PCA降维
#    - 使用更好的距离度量
```

---

## 练习9：核函数可视化（项目）

### 题目描述
理解核函数如何将低维非线性问题转化为高维线性问题。

### 任务
```python
# 1. 使用XOR数据（经典的非线性不可分问题）
X = np.array([
    [0, 0], [0, 1], [1, 0], [1, 1]
])
y = np.array([0, 1, 1, 0])

# 2. 分别用Linear、RBF、Poly核训练SVM
# 3. 尽管数据只有4个点，仍尝试绘制决策边界
# 4. 讨论：
#    - 为什么线性核无法分类XOR？
#    - RBF和Poly为什么能够分类？
#    - 核函数背后的几何意义是什么？
```

---

## 挑战题：从零实现KNN

### 题目
手写实现KNN算法，不使用sklearn的KNeighborsClassifier。

### 任务
```python
class KNNFromScratch:
    def __init__(self, k=3):
        self.k = k
        self.X_train = None
        self.y_train = None
    
    def fit(self, X, y):
        """存储训练数据"""
        self.X_train = X
        self.y_train = y
    
    def predict(self, X):
        """预测"""
        predictions = []
        for x in X:
            # 计算与所有训练样本的距离
            # 找k个最近的邻居
            # 投票
            pass
        return np.array(predictions)

# 1. 实现上述方法
# 2. 测试在虹膜数据集上的性能
# 3. 与sklearn的KNN对比
# 4. 讨论效率问题（为什么sklearn更快？）
```

---

## 提交要求

对于每道练习题：
1. 编写完整的Python代码
2. 添加详细的注释
3. 包含数据可视化
4. 写出分析和结论

完成后对照 `04-答案详解.py` 检查答案。

## 学习建议

- **练习1-3**：适合初学者，巩固KNN基础
- **练习4-6**：适合有一定基础，想深入理解SVM
- **练习7-8**：适合准备实际应用
- **练习9-挑战题**：适合追求深度理解的学习者

**记住：KNN的关键是特征缩放，SVM的关键是核函数选择！**

